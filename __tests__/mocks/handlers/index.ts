import { http, HttpResponse } from "msw";

// MSW handlers for Supabase API mocking
// These will be populated as we build out features

export const handlers = [
  // Auth handlers
  // Group handlers
  // Order handlers
  // etc.
];
